class Game

	#static variables
	@instance 		= new Game()
	@stage			= null

	paused			: true

	constructor:()->
		return

	init:()->
		return

	start:()->
		return

	update:()->
		if @paused
			return
		
		return
